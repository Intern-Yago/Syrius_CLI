import React from 'react';
import styles from './styles.module.css';

export interface <%= props.name[0].toUpperCase() + props.name.substring(1) %>Props {
  children?: React.ReactNode;
  className?: string;
}

export const <%= props.name[0].toUpperCase() + props.name.substring(1) %>: React.FC<<%= props.name[0].toUpperCase() + props.name.substring(1) %>Props> = ({
  children,
  className = '',
}) => {
  return (
    <div className={`${styles.container} ${className}`}>
      {children || <span><%= props.name %> Component</span>}
    </div>
  );
};

export default <%= props.name[0].toUpperCase() + props.name.substring(1) %>;
