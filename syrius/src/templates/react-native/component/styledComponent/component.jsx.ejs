import React from 'react'
import { View} from "react-native";
//import {Container} from './styles'

export default function <%= props.name[0].toUpperCase()+props.name.substring(1)%>(){
  return(
    <Container>
      <View>
      </View>
    </Container>
  );
}