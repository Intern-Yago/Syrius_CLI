<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sobre — <%= props.name[0].toUpperCase() + props.name.substring(1) %></title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="./styles.css">
</head>
<body>
  <header class="header">
    <div class="container nav-container">
      <a href="./index.html" class="logo"><%= props.name[0].toUpperCase() + props.name.substring(1) %></a>
      <nav class="nav">
        <a href="./index.html">Início</a>
        <a href="./about.html" class="active">Sobre</a>
      </nav>
    </div>
  </header>

  <main class="main container">
    <section class="content-card">
      <h1>Sobre o Projeto <%= props.name[0].toUpperCase() + props.name.substring(1) %></h1>
      <p>Esta é uma página de exemplo gerada automaticamente pela <strong>Syrius CLI</strong>.</p>
      <p>Você pode customizar o layout, adicionar estilos adicionais e incluir novos scripts JS para expandir sua aplicação.</p>
      <br>
      <a href="./index.html" class="btn btn-secondary">&larr; Voltar para o Início</a>
    </section>
  </main>

  <footer class="footer">
    <div class="container">
      <p>&copy; 2026 <%= props.name[0].toUpperCase() + props.name.substring(1) %>. Criado com Syrius CLI.</p>
    </div>
  </footer>

  <script src="./scripts.js"></script>
</body>
</html>