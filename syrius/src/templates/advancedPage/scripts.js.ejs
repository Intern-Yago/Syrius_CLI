// Script de exemplo para <%= props.name %>
document.addEventListener('DOMContentLoaded', () => {
  console.log('🚀 Página <%= props.name %> carregada com sucesso via Syrius CLI!');

  const btnAlert = document.getElementById('btn-alert');
  if (btnAlert) {
    btnAlert.addEventListener('click', () => {
      alert('✨ Interatividade JS funcionando perfeitamente em <%= props.name %>!');
    });
  }
});